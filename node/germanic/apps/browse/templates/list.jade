//-
//- The list template that renders the pages. This template is
//- used on the server via `include` in the index.jade file, and
//- this same template can be used on the client via
//- [Jadeify](https://github.com/domenic/jadeify).
//-

for page in pages
    li
        a(href='/pages/' + page.get('page_id'))
            = page.get('header')
